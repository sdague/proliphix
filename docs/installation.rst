.. highlight:: shell

============
Installation
============

At the command line::

    $ easy_install proliphix

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv proliphix
    $ pip install proliphix
